#!/bin/sh
set -eu

: "${1:?usage: kiosk-provision-credential SOURCE_JSON}"
install -m 0600 -o root -g root "$1" /etc/kiosk/kiosk-credential.json
echo "credential installed; verify with: stat -c '%a %U:%G' /etc/kiosk/kiosk-credential.json"
