#!/bin/sh
set -e

if [ -x "$(command -v deb-systemd-invoke 2>/dev/null || true)" ] &&
   [ -z "${DPKG_ROOT:-}" ] && [ -d /run/systemd/system ]; then
    deb-systemd-invoke stop kiosk.service || true
fi

exit 0
